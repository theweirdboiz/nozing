import React from "react";
import PropTypes from "prop-types";

const Skeleton = (props) => {
  return <div className="absolute inset-0 w-full h-full bg-vip"></div>;
};

Skeleton.propTypes = {};

export default Skeleton;
