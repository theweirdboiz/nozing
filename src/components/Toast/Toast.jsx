import React from "react";
import PropTypes from "prop-types";

const Toast = (props) => {
  return (
    <div className="fixed flex items-center justify-center bg-sidebar-bg z-50">
      Toast ne
    </div>
  );
};

Toast.propTypes = {};

export default Toast;
