// home selector
export const homeSelector = (state) => state.explore;

export const songSelector = (state) => state.songs;

// playlist slice
export const playlistSelector = (state) => state.playlists;

// audio slice
export const playerSelector = (state) => state.player;
