import React from "react";
import PropTypes from "prop-types";

const Player = () => {
  return (
    <section className="fixed bottom-0 left-0 right-0 container">
      Player
    </section>
  );
};

Player.propTypes = {};

export default Player;
